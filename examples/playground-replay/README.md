# Playground replay fixture

The `secret-exfil-url-blocked/` fixture contains the evidence, manifest, packet, summary, and verifier output used by the [playground replay capture guide](../../docs/guides/playground-replay-capture.md). Follow that guide for the capture and verification workflow. `signing-key.pub` is the fixture's public verification key.
