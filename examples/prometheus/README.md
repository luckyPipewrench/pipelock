# Prometheus alert rules

[`pipelock-alerts.yaml`](pipelock-alerts.yaml) contains the alert rules referenced by the [SIEM integration guide](../../docs/guides/siem-integration.md). Follow that guide to configure metrics and connect the alerts to your monitoring system.
